= content_for :header do
  %h1 Welcome to #{active_league.name}!

%h2 Players
%ul
  - active_league.players.each do |p|
    %li= link_to p.name.titleize, p

%h2 Games
%table.characters
  %tr
    %th
    - Character.all.each do |c|
      %th= link_to image_tag("character-icons/#{c.name.downcase}.png", :class => 'thumbnail'), c
  - Character.all.reverse.each do |c1|
    %tr
      %th= link_to image_tag("character-icons/#{c1.name.downcase}.png", :class => 'thumbnail'), c1
      -Character.all.each do |c2|
        %td{:style => "background-color: #{wins_color(c1,c2,active_league)}"}
          .total-matches.no-hover= link_to c1.games_against(c2, active_league).size.to_s, character_versus_path(c1, c2, active_league)
          .win-loss.hover=link_to win_loss(c1,c2,active_league), character_versus_path(c1, c2,active_league)
  %tr
    %th
    - Character.all.each do |c|
      %th= link_to image_tag("character-icons/#{c.name.downcase}.png", :class => 'thumbnail'), c